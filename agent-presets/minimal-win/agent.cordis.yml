# The `minimal` agent preset: a fixed-prompt, two-tool coding-agent composition.
#
# The persona is the complete system prompt, so global identity, Web orientation,
# tool guidance, and later assembly listeners cannot add prompt text. Runtime
# context snapshots are suppressed for this preset, and the model composes only
# one shell tool plus `str_replace_editor`. Context compaction is absent.
#
# Platform branches: the persistent shell is a POSIX bash PTY stack, so it is
# Linux/macOS-only. On win32 every row of the persistent-shell group below is
# disabled and the preset mounts the host's sandboxed `pwsh` tool instead
# (each call is a fresh pwsh process; no persistent shell is offered on Windows).

- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    text: You are a helpful software engineer assistant.
    complete: true
    includeRuntimeContext: false

# The PTY registry is an agent-owned service, so it lives in an entry-local
# realm. The backend still consumes the host sandbox policy and subprocess
# implementation, while the tool registers into this agent's scoped catalog.
#
# POSIX-only: `dsh-terminal-bash` defaults to shellPath `/bin/bash`, which does
# not exist on Windows. Each row is disabled on win32 (the same per-row switch
# the standard preset uses for its bash tool) and the `tool-pwsh` row below
# covers Windows instead.
- id: persistent-shell
  name: cordis:group
  group: true
  isolate:
    terminals: true
  config:
    - id: pty
      name: '@deepseek-ai/dsh-terminal'
      disabled: !!js process.platform === 'win32'

    - id: terminal-bash
      name: '@deepseek-ai/dsh-terminal-bash'
      disabled: !!js process.platform === 'win32'
      config:
        timeoutMs: 300000

    - id: persistent-bash
      name: '@deepseek-ai/dsh-tool-bash-persistent'
      disabled: !!js process.platform === 'win32'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a bash shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * You do have access to a mirror of common linux and python packages via apt and pip.
          * State is persistent across command calls and discussions with the user.
          * To inspect a particular line range of a file, e.g. lines 10-25, try 'sed -n 10,25p /path/to/the/file'.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'sleep 10 &' or start a server in the background.

# Windows branch: the bash rows above are Linux-only. Mount the host's
# sandboxed pwsh tool (each call is a fresh pwsh process) so the two-tool
# surface stays available on win32. Resolves host-plane `shell`/`systemPrompt`/
# `shellEnv`, exactly like the standard preset's tool-pwsh row. Background
# execution is turned off: this preset mounts no `job_*` controls, so an
# advertised background run could never be collected.
- id: tool-pwsh
  name: '@deepseek-ai/dsh-tool-pwsh'
  disabled: !!js process.platform !== 'win32'
  config:
    enableRunInBackground: false

# The bare local filesystem shadows the host's sandboxed provider only for this
# preset. The editor shares that realm and requires absolute paths.
- id: filesystem
  name: cordis:group
  group: true
  isolate:
    fs: true
  config:
    - id: fs-local
      name: '@deepseek-ai/dsh-fs-local'
      config:
        cwd: !!js process.env.DSH_CWD ?? process.cwd()

    - id: str-replace-editor
      name: '@deepseek-ai/dsh-tool-str-replace-editor'
      config:
        maxOutputChars: 16000
